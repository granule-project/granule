import File
import Sequence

main : Sequence Char
main =
    let h          = openFile "LICENSE" ReadMode
    in let (h, c)  = hGetChar h
    in let (h, c') = hGetChar h
    in let ()      = hClose h
    in (Next c (Next c' Empty))
