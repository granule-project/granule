import File

main : String <[O,RW,RW,C]>
main =
    let h       <- openFile "LICENSE" ReadMode;
        (h, c)  <- hGetChar h;
        (h, c') <- hGetChar h;
        ()      <- hClose h
    in pure ((showChar c) `stringAppend` (showChar c'))
