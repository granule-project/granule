main : String <IO>
main =
    let h1       <- openFile "LICENSE" ReadMode;
        (h2, c)  <- hGetChar h1;
        (h3, c') <- hGetChar h2;
        ()       <- hClose h3
    in pure ((showChar c) `stringAppend` (showChar c'))
