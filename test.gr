data Signal a = Tick a (Signal a)

data Addr = Ad Int

ram : Signal (Addr, Int) -> Signal (Addr, Int [])
ram = ram

system : Int
system input =
    let writePort = writePort in
    let readPort = ram writePort in 1
