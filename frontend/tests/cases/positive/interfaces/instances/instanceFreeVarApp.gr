interface Simple a b

-- application of type variable to type variable
--
-- we should be able to infer that 'f' has kind 'Type -> Type',
-- and 'x' has kind 'Type' (this is the only way we can satisfy
-- this for both parameters to fit the kind of the interface)
instance Simple (f x) x
