interface Simple1 a where
  ignore1 : a -> a

interface Simple2 a where
  ignore2 : a -> a

-- a function with multiple interface constraints
simple : forall (a : Type) . {Simple1 a, Simple2 a} => a -> a
simple x = x
