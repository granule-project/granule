language GradedBase 

data List a = Cons a (List a) | Nil

concat : forall {a : Type} . (List a) %Any -> (List a) %Any ->  List a
spec 
    concat (Cons 0 Nil) Nil = (Cons 0 Nil);
    concat (Cons 0 Nil) (Cons 1 Nil) = (Cons 0 (Cons 1 Nil));
    concat %Any
concat = ?