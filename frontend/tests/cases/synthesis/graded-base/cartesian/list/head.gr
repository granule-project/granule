language GradedBase

data List a = Cons a (List a) | Nil 

data Maybe a = Just a | Nothing 
head : forall a . (List a) %Any -> a %Any -> a
spec 
    head Nil 2 = 2;
    head (Cons 1 (Cons 2 Nil)) 2 = 1;
head = ?