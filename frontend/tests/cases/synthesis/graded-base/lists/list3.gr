language GradedBase

data List a = Nil | Cons a (List a) 

concat : forall a . List a % 0..1 -> List a % 1..∞ -> List a
concat = ?
-- concat (Cons x xs) ys = Cons x (concat xs ys);
-- concat Nil ys = ys